# GestureKit Agent 工作约定

本文件是仓库级执行入口，只记录所有模块都必须遵守的约束。具体流程、模板和产品事实以文末链接的权威文件为准，避免在此重复维护。

## 1. 工作边界

- 项目文档默认使用中文；代码、命令、协议字段和路径保留原始语言。
- 动手前先读取与任务相关的产品契约、架构文档和现有实现；产品范围以 `docs/product/` 中对应版本的契约为准。
- 不扩大当前已验证的手势、浏览器动作或 Provider 协议范围。需要扩展时，先更新契约、方案和验收条件。
- 不撤销、覆盖或混入用户已有的未提交改动；遇到直接冲突时停止并说明冲突。

## 2. 开发流程

- 功能、修复和文档变更均在独立分支完成，通过 Pull Request 合并到 `main`；不得直接向 `main` 提交或推送。
- 分支使用 `<type>/<简短主题>`；提交按可独立理解、测试和回退的逻辑单元拆分。
- 已明确的后续步骤应连续执行。仅在需要产品决策、遇到真实阻塞，或准备执行合并、发布、删除、推送等不可逆操作时中断请求确认。
- 进展必须基于实际命令、修改或验证结果；不得以“正在处理”代替未执行的工作。

## 3. 质量与验证

- 修改前理解调用链和现有测试；功能或缺陷修改优先补充能覆盖行为的测试。
- 完成前运行与改动相关的自动化验证；涉及 macOS App 或 Chrome 扩展的用户行为时，提供可执行的手动验证步骤。
- 只有在命令输出确认通过后，才能声明构建、测试或缺陷修复完成；如有警告、未覆盖路径或无法验证的内容，明确说明。

## 4. 日志与诊断

- 日志分为三层：关键操作摘要默认记录；失败诊断胶囊仅在失败时自动保留；完整诊断轨迹仅在用户开启诊断日志后记录。
- 关键操作摘要必须可独立定位大多数故障：至少包含关联的 session/operation 标识、手势或动作、上下文结果、关键保护或前置条件结果、终态、失败阶段和总耗时。不得仅记录泛化错误码。
- 失败诊断胶囊必须关联同一 session/operation，保留失败前后的有限关键阶段；完整诊断轨迹记录阶段走向与耗时，供复现竞态和代码流程排查。
- 默认路径不得记录逐帧触控板输入、网页正文、完整 URL、Cookie、认证材料或无上限调试输出。诊断记录必须脱敏、按单次操作条数/字节数限额截断，并受 7 天/50 MB 受管理存储预算约束。
- 新增异步、多进程或跨组件流程时，必须同时补关键摘要、失败阶段和诊断阶段日志，并以测试覆盖关键失败分支；不得只依赖控制台临时输出。

## 5. 状态机与跨组件会话

- 修改手势识别、异步动作或跨进程流程前，必须列出状态转换：开始条件、成功终态、每一种中断/超时/配置变更终态，以及每个终态的资源释放动作。
- 每一个 `candidateStarted`、guard arm、请求登记等资源创建事件，必须在所有退出路径恰好对应一次成功或拒绝终态；禁止通过静默 `return`、直接清空局部变量或覆盖状态来结束已发布的候选。
- 终态处理必须幂等，并负责清理本地状态、待处理队列、定时器和跨组件 guard；清理失败或过期不得遗留可被下一次操作复用的状态。
- 跨组件生命周期默认携带同一个 correlation/session ID。不得为新增流程引入按到达顺序、全局“当前对象”或时间猜测的关联方式；现有 FIFO 兼容路径修改时，必须补充错序与中断后的回归测试。
- 每次状态机修改至少覆盖：正常完成、每类中断、超时/过期、同一物理操作内禁止重新入场，以及“中断后紧接一次正常操作”。测试应同时断言用户可见结果和 session/guard 是否已释放。
- 代码评审和 PR 描述必须给出状态转换表或等价测试清单；没有终态闭合和释放验证的状态机改动不得合并。

## 6. 本地质量材料

- `docs/quality/` 用于本地排障记录与质量回溯，目录内容默认被 Git 忽略；未经用户明确要求，不得暂存、提交或在 PR 中引用其中的材料。
- 需要对外发布的复盘、设计决策或操作规范，应提炼脱敏后的结论，分别写入 `docs/adr/`、`docs/architecture/` 或 `docs/operations/`，不得直接提交本地原始排障材料。

## 7. 协作与发布

- PR 必须说明背景、用户可感知变化、兼容性和风险、验证结果及发布说明草案。
- 未经用户明确授权，不得自行合并 PR、删除远端分支、创建版本标签或发布 GitHub Release。
- 功能版本应说明新增能力、体验改进、相对上一版本的修复和迁移；补丁版本应聚焦修复问题、影响范围和验证。

## 8. 权威资料

- [提交与发布规范](docs/contributing/commit-and-release-guide.md)
- [PR 模板](.github/PULL_REQUEST_TEMPLATE.md)
- [发布说明模板](docs/releases/RELEASE_TEMPLATE.md)
- [产品契约](docs/product/)
- [架构与决策记录](docs/architecture/) 与 [ADR](docs/adr/)
