---
状态: in-progress
创建: v2.5
最后更新: 2026-08-09
关联:
  - ../architecture/v2.5-手势绑定与手势系统-设计.md
  - ../product/v2/v2.4-可靠性契约.md
---

# V2.5 手势绑定与手势系统开放 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐项实现。Steps 使用 `- [ ]` 勾选跟踪。

**版本语义：** V2.5 为**功能版本**：开放手势→动作绑定（用户可配映射）+ 开放预设手势集（识别器支持 2/3/4 指）。在 v2.4.2 文档整改后的代码库上实现。新增 Provider 标准动作（向后兼容 case），不破坏现有三指手势识别。

**目标：** 用户在控制中心可配置"手势 → 动作"映射并立即生效；识别器支持预设多指手势（二指左右滑动、四指轻点/滑动等）；现有 6 个三指手势识别不退化。

**设计依据：** `docs/architecture/v2.5-手势绑定与手势系统-设计.md`（grilling 共识 + 现状核验）。

## 范围

**包含：**
- 绑定开放：手势→动作映射可配置（现有 7 个标准动作 + 新增 Tab 管理类 + 链接页面类），上下文约束系统预设
- 手势开放：预设手势集（二指左右滑动、四指轻点/滑动），识别器增量参数化
- 系统手势检测/引导（二指左右滑 → 提示关闭系统「页面间轻扫」）
- 控制中心绑定配置 UI

**非目标：**
- DIY 手势编辑器（任意手指数/原语/方向/区域组合）
- 上下文约束（链接上 vs 空白处）开放给用户
- 进阶动作（翻译/深色模式/分屏/TTS/下载）——延后单独排

## 全局约束（沿用既有）

- 范围与验收依据：`docs/product/v2/v2.4-可靠性契约.md`；Provider 协议新增为向后兼容 case。
- 每项实现遵循 TDD：先运行失败测试 → 最小实现 → 任务级与相关全量测试。
- 项目文档中文优先；代码、命令、协议字段和路径保持原文。
- 新增 Chrome 权限（clipboard/downloads 等）需同步扩展 manifest 与安装说明。

---

## 任务

### Task 1：绑定配置模型与持久化

- [x] **Step 1**：新增用户绑定配置模型（基于 `BindingRule`，默认 `DefaultRules.v1Bindings` + 用户覆盖；支持启用/禁用单条）。
- [x] **Step 2**：`SettingsStore` 持久化用户绑定（增量键，缺省回退默认）；TDD 覆盖保存/加载/回退。
- [x] **Step 3**：`RuleEngine` 接入用户绑定（默认 + 覆盖合并，优先级保留）；TDD 覆盖覆盖生效与回退。
- [x] **Step 4**：提交。

### Task 2：新增标准动作（Tab 管理 + 链接页面）

- [x] **Step 1**：Core `StandardActionID` 扩展新增动作：Tab 管理类（`browser.tab.open_new` / `browser.tab.pin` / `browser.tab.unpin` / `browser.tab.toggle_mute` / `browser.tab.close_others` / `browser.tab.restore`）+ 链接页面类（`browser.link.copy` / `browser.page.copy_url` / `browser.page.scroll_top_bottom`）。*实施与文档统一使用 namespaced 动作 ID（见结论「命名偏差（已同步）」）。*
- [x] **Step 2**：Provider 协议 v2 新增动作定义（类型↔payload 映射，向后兼容）；schema + TS 类型同步。
- [x] **Step 3**：Chrome 扩展侧执行实现（`chrome.tabs/sessions/clipboard` 等）；manifest 权限同步（`sessions`、`clipboardWrite` 已声明）。
- [x] **Step 4**：TDD：协议解码测试 + 扩展侧单测（vitest）+ 真实 Chrome 冒烟（新动作触发）。
- [x] **Step 5**：提交。

### Task 3：识别器增量参数化 + 预设手势集

- [x] **Step 1**：`GestureRecognizer.observe`：`fingerCount == 3` 写死 → 按活跃手势定义集匹配 fingerCount；保留 session 状态机语义（升高→reject、降低→classify、抬指→classify）。
- [x] **Step 2**：`classify` 按命中定义的 primitive/direction/region 输出对应 `ComposedGesture`。
- [x] **Step 3**：新增预设 `GestureDefinition`：二指左右滑动、四指轻点、四指左右滑动（避开系统占用的取舍见设计 §7）。
- [x] **Step 4**：`ComposedGesture`/`GestureType` 新增对应 case（向后兼容）。
- [x] **Step 5**：TDD：现有 6 个三指手势识别测试**全绿保底** + 新增二指/四指识别测试（先红后绿）。
- [x] **Step 6**：提交。

### Task 4：控制中心绑定 UI + 系统手势引导

- [x] **Step 1**：控制中心绑定配置页（手势列表 → 动作下拉选择；默认/用户覆盖态显示；启用开关）。
- [x] **Step 2**：立即生效（改配置 → RuleEngine 热更新）。
- [x] **Step 3**：系统手势检测：读取 macOS「在页面间轻扫」设置（`defaults read` 触控板设置）；绑定二指左右滑时若开启则提示引导关闭。
- [x] **Step 4**：TDD/UI 测试（SwiftUI + 数据源单测）。
- [x] **Step 5**：提交。

### Task 5：全量验证与真实 Chrome 门

- [x] **Step 1**：`swift test`、`npm test`、`npm run build`、node e2e 单测、dry-run 全绿。
- [x] **Step 2**：本机真实 Chrome：绑定重绑生效验证 + 新动作（Tab 管理/链接页面）触发验证 + 二指左右滑（关闭系统「页面间轻扫」后）不冲突。*物理触控板新手势无法 E2E 自动化，见结论「需人工验收」。*
- [ ] **Step 3**：CI（PR 触发）质量门绿。*待 PR 触发后验证。*
- [ ] **Step 4**：提交并开 PR 到 main（v2.5）。*待授权。*

---

## 验收标准

- 用户可在控制中心重绑「手势 → 动作」，保存后立即生效；默认绑定可回退。
- 现有 6 个三指手势识别不变（识别测试全绿）；新增二指左右滑动、四指轻点/滑动可用。
- 二指左右滑（系统「页面间轻扫」关闭后）触发绑定动作，不与系统滚动冲突；App 有检测与引导。
- 新增动作（Tab 管理类/链接页面类）在真实 Chrome 生效，协议向后兼容。
- 通用 CI（swift/extension/scripts）全绿。
- 无 token/凭据泄漏；无残留进程或临时目录。

---

## 结论（2026-08-09 全量验证）

Task 1-4 已完成并通过全量验证；Task 5 Step 1/2 完成，Step 3（CI PR 质量门）与 Step 4（提交开 PR）待授权/待 PR。

### 验证矩阵

| 项 | 命令 | 结果 |
|---|---|---|
| Swift 全量 | `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test` | **226 passed / 0 failed** |
| 扩展单测 | `npm test`（vitest，jsdom） | **203 passed / 0 failed**（26 文件） |
| 扩展构建 | `npm run build`（esbuild） | 成功（`dist/` 产物更新） |
| Node E2E 单测 | `node --test scripts/e2e/test-link-reliability.mjs` | **60 passed / 0 failed** |
| 干跑 | `zsh scripts/dev/test-link-reliability.sh --dry-run` | **exit 0** |

### 真实 Chrome 门（回归，headless 单次）

`node scripts/e2e/run-link-reliability.mjs --headless` 退出码 **0**，四场景 terminalStatus 全绿：

| 场景 | terminalStatus |
|---|---|
| success | `succeeded` |
| leaseExpiry | `lease_released` |
| providerUnavailable | `provider_unavailable_guard_released` |
| resultUnknown | `result_unknown` |

运行结束已清理 GestureKitApp/Chrome 进程与临时目录；未触碰用户级真实 manifest（E2E 使用独立 host 名 `com.gesturekit.host.e2e` 与临时 profile）。

### 新动作（Tab 管理 + 链接页面）

- 9 个新标准动作在 Core `StandardActionID`（16 个，含既有 7 个）、Provider 协议 v2（Swift/TS/schema）、扩展 `actionExecutor` 与 build 产物中一致存在：`browser.tab.open_new` / `browser.tab.pin` / `browser.tab.unpin` / `browser.tab.toggle_mute` / `browser.tab.close_others` / `browser.tab.restore`、`browser.link.copy`、`browser.page.copy_url` / `browser.page.scroll_top_bottom`。
- **命名偏差（已同步）**：实施采用 namespaced 动作 ID（`browser.tab.open_new` 等）；计划/设计文档中原有的简写名（`open_new_tab`、`copy_link` 等）已统一为 namespaced 全名。
- 新权限 `sessions`（恢复关闭标签页）、`clipboardWrite`（复制链接/URL）已在 `manifest.json` 声明，并同步到 README 与安装说明。
- 扩展侧单测覆盖执行逻辑：`actionExecutor` 22、`chromeProvider` 27、`pointerTracker` 22。

### 绑定配置生效

- Core `UserBindingConfigurationTests`（14 用例）+ App `RuntimeBindingTests`（7 用例）覆盖 `updateGestureBinding` 持久化覆盖、**立即生效**（热更新 RuleEngine）、覆盖/禁用/回退/恢复默认。
- 控制中心绑定页（手势列表 → 动作下拉 + 启用开关 + 用户覆盖态显示）已实现，`ControlCenterPresentationTests` 覆盖数据源展示与系统手势冲突标记。

### 系统手势检测与边界（需人工验收）

- `SystemGestureDetector` 读取内置 + 蓝牙触控板偏好（`TrackpadFourFingerHorizSwipeGesture` 等）。本机实测：四指「在应用之间轻扫」= **2（开启，两个偏好域一致）**；三指「页面间轻扫」= 0（关闭）。
- **四指滑动受系统手势冲突影响**（「在应用之间轻扫」开启），本结论未强行验证；需用户关闭该设置后人工验收。
- **二指/四指新手势的物理触发无法在 E2E 自动化**（无触控板输入模拟），标注为**需人工验收**（见人工验收清单）。
- 三指现有 6 手势识别测试全绿，未退化。

### CI 与提交

- Step 3（CI PR 质量门）待开 PR 后验证；Step 4（提交并开 PR）待授权。

---

## 人工验收清单

以下项需在真实触控板上由人工验收（E2E 无法模拟触控板物理输入）：

1. **二指左右滑动**：确认系统「在页面间轻扫」已关闭后，绑定动作可触发（如切换到下一个标签页）。
2. **四指轻点**：确认绑定动作触发（不与系统 Mission Control 冲突）。
3. **四指左右滑动**：本机「在应用之间轻扫」=2（开启）→ 需先在系统设置关闭该手势，再验收绑定动作触发。
4. **链接页面类动作在真实网页**：复制链接（`browser.link.copy`）、复制 URL（`browser.page.copy_url`）、滚动顶底（`browser.page.scroll_top_bottom`）在真实 Chrome 页面的粘贴/滚动效果。
5. **Tab 管理类动作**：开新标签、固定/取消固定、静音切换、关闭其他、恢复关闭标签在真实 Chrome 的行为（`sessions`/`clipboardWrite` 权限下）。

---

## 修订记录

- **2026-08-09（全量验证）**：Task 1-4 与 Task 5 Step 1/2 完成。验证矩阵全绿；真实 Chrome 门（headless）四场景全过；新动作/绑定配置经单测 + 构建产物确认。物理触控板新手势标注需人工验收；四指滑动因本机「在应用之间轻扫」开启受系统冲突影响。Step 3/4 待 PR 与授权。
- **2026-08-08（创建）**：基于 grilling 共识（绑定开放 + 预设手势集 + 增量参数化 + 系统手势策略）+ 工作量评估（约 14-22 人日 / 3000-5500 行 / token 低成本）落成本计划。设计见 `../architecture/v2.5-手势绑定与手势系统-设计.md`。
